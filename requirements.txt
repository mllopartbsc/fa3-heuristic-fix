# Core dependencies for reproduction
torch>=2.4.0
numpy>=1.24.0
pyyaml>=6.0

# FlashAttention-3 (installed from source — see setup_environment.sh)
# flash-attn>=3.0.0

# Optional: for Nsight Compute profiling analysis
# pandas (for CSV parsing)
